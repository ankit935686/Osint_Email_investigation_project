# 🏗️ System Architecture & Flow Diagrams

## Table of Contents
1. [High-Level Architecture](#high-level-architecture)
2. [Request Flow Diagram](#request-flow-diagram)
3. [Module Structure](#module-structure)
4. [Data Flow](#data-flow)
5. [Async Execution Model](#async-execution-model)

---

## 1. High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER'S BROWSER                          │
│  ┌────────────┐  ┌──────────────┐  ┌─────────────────────┐    │
│  │  HTML/CSS  │  │  JavaScript  │  │  Bootstrap/FA Icons │    │
│  │  Template  │  │  AJAX/Fetch  │  │      Styling       │    │
│  └────────────┘  └──────────────┘  └─────────────────────┘    │
└──────────────────────────┬──────────────────────────────────────┘
                           │ HTTP Requests (REST API)
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│                    FLASK WEB SERVER (app.py)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐ │
│  │   Routes     │  │    Cache     │  │  Background Thread   │ │
│  │ /check_email │  │ results_cache│  │  run_holehe_async()  │ │
│  │ /get_results │  │  (in-memory) │  │                      │ │
│  └──────────────┘  └──────────────┘  └──────────────────────┘ │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ Trio.run()
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│                ASYNC EXECUTOR (holehe/core.py)                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Module Discovery & Loading                  │  │
│  │    import_submodules() → get_functions()                │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │         HTTPX Async Client (Connection Pool)            │  │
│  └──────────────────────────────────────────────────────────┘  │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ Concurrent Async Calls
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│              WEBSITE CHECKING MODULES (120+)                    │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ │
│  │Instagram│ │ Google  │ │ Twitter │ │ Netflix │ │  ...    │ │
│  │  .py    │ │  .py    │ │  .py    │ │  .py    │ │  (120+) │ │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘ │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ HTTPS Requests
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│                    EXTERNAL WEBSITES                            │
│  Instagram.com │ Google.com │ Twitter.com │ Netflix.com │ ...   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. Request Flow Diagram

### User Initiates Search:

```
┌─────────┐
│  USER   │ Enters email: "test@gmail.com"
└────┬────┘
     │
     ▼
┌────────────────────┐
│   BROWSER (JS)     │
│ 1. Form Submit     │
│ 2. Show Loading    │
└─────────┬──────────┘
          │
          │ POST /check_email
          │ {"email": "test@gmail.com"}
          │
          ▼
┌─────────────────────────────┐
│   FLASK SERVER (app.py)     │
│ 1. Validate email           │
│ 2. Check cache              │
│ 3. Mark as "processing"     │
│ 4. Start background thread  │────────┐
└─────────┬───────────────────┘        │
          │                            │
          │ Response:                  │
          │ {"status": "processing"}   │
          │                            │
          ▼                            ▼
┌─────────────────────┐    ┌────────────────────────────┐
│   BROWSER (JS)      │    │  BACKGROUND THREAD         │
│ Start Polling:      │    │  run_holehe_async(email)   │
│                     │    │                            │
│ Every 1 second:     │    │  1. Import modules         │
│ GET /get_results/   │    │  2. Create async client    │
│     test@gmail.com  │    │  3. Run trio.run(main)     │
│                     │    └──────────┬─────────────────┘
│ Check status:       │               │
│ • processing?       │               ▼
│   → keep polling    │    ┌────────────────────────────┐
│ • completed?        │    │   TRIO ASYNC RUNTIME       │
│   → display results │    │   120+ concurrent tasks    │
│ • error?            │    │                            │
│   → show error      │    │   await instagram(email)   │
└─────────────────────┘    │   await google(email)      │
                           │   await twitter(email)      │
                           │   await netflix(email)      │
                           │   ... (117 more)           │
                           └──────────┬─────────────────┘
                                      │
                                      │ Results collected
                                      ▼
                           ┌────────────────────────────┐
                           │   RESULTS CACHE            │
                           │   results_cache[email] = { │
                           │     status: "completed",   │
                           │     results: [...],        │
                           │     found_accounts: 15     │
                           │   }                        │
                           └──────────┬─────────────────┘
                                      │
                                      │ Retrieved by polling
                                      ▼
                           ┌────────────────────────────┐
                           │   BROWSER DISPLAYS         │
                           │   • Statistics             │
                           │   • Result cards           │
                           │   • Filter options         │
                           │   • Export button          │
                           └────────────────────────────┘
```

---

## 3. Module Structure

```
holehe/
│
├── app.py ────────────────────── Flask Application Entry Point
│   ├── @app.route('/')         └─ Render index.html
│   ├── @app.route('/check_email')  └─ Start async check
│   └── @app.route('/get_results')  └─ Return cached results
│
├── templates/
│   └── index.html ────────────── Web Interface
│       ├── HTML Structure
│       ├── CSS Styling (embedded)
│       └── JavaScript Logic
│           ├── Form submission
│           ├── Polling mechanism
│           ├── Result display
│           └── Filter/Export functions
│
└── holehe/ ───────────────────── Core Package
    │
    ├── __init__.py ──────────── Package initialization
    │
    ├── core.py ──────────────── Core Logic
    │   ├── import_submodules()  └─ Dynamic module discovery
    │   ├── get_functions()      └─ Extract callable functions
    │   ├── is_email()           └─ Email validation
    │   ├── launch_module()      └─ Execute single check
    │   └── maincore()           └─ CLI entry point
    │
    ├── instruments.py ───────── Progress Tracking
    │   └── TrioProgress class   └─ tqdm progress bar
    │
    ├── localuseragent.py ───── User Agent Strings
    │   └── ua dictionary        └─ Browser user agents
    │
    └── modules/ ─────────────── Website Checking Modules
        │
        ├── social_media/
        │   ├── instagram.py ──── Instagram checker
        │   ├── twitter.py ────── Twitter checker
        │   ├── facebook.py ───── Facebook checker
        │   └── ... (20+ more)
        │
        ├── mails/
        │   ├── google.py ─────── Gmail checker
        │   ├── yahoo.py ──────── Yahoo checker
        │   └── ... (5 total)
        │
        ├── shopping/
        │   ├── amazon.py ─────── Amazon checker
        │   ├── ebay.py ───────── eBay checker
        │   └── ... (10+ total)
        │
        ├── crm/
        │   ├── hubspot.py ────── HubSpot checker
        │   ├── zoho.py ───────── Zoho checker
        │   └── ... (10 total)
        │
        └── ... (20+ more categories)
            └── 120+ total modules
```

---

## 4. Data Flow

### A. Email Check Data Flow:

```
INPUT: "test@gmail.com"
   │
   ▼
┌──────────────────────────────────────┐
│  1. VALIDATION                       │
│  • Format check (regex)              │
│  • Length validation                 │
└───────────────┬──────────────────────┘
                │
                ▼
┌──────────────────────────────────────┐
│  2. MODULE DISCOVERY                 │
│  • Scan holehe/modules/              │
│  • Import all .py files              │
│  • Extract async functions           │
│  Result: [instagram, google, ...]   │
└───────────────┬──────────────────────┘
                │
                ▼
┌──────────────────────────────────────┐
│  3. ASYNC EXECUTION                  │
│  For each module:                    │
│    • await module(email, client, out)│
│    • HTTP requests to website        │
│    • Parse response                  │
│    • Append result to 'out'          │
└───────────────┬──────────────────────┘
                │
                ▼
┌──────────────────────────────────────┐
│  4. RESULT AGGREGATION               │
│  out = [                             │
│    {"name": "instagram",             │
│     "exists": True, ...},            │
│    {"name": "google",                │
│     "exists": False, ...},           │
│    ... (120 results)                 │
│  ]                                   │
└───────────────┬──────────────────────┘
                │
                ▼
┌──────────────────────────────────────┐
│  5. PROCESSING & STORAGE             │
│  • Count found accounts              │
│  • Calculate statistics              │
│  • Store in cache                    │
│  • Mark as "completed"               │
└───────────────┬──────────────────────┘
                │
                ▼
OUTPUT: {
  "status": "completed",
  "total_sites": 120,
  "found_accounts": 15,
  "results": [...]
}
```

### B. Individual Module Data Flow (Example: Instagram):

```
INPUT: email = "test@gmail.com"
   │
   ▼
┌─────────────────────────────────────┐
│  STEP 1: GET CSRF TOKEN             │
│  GET instagram.com/accounts/signup/ │
│  Extract: csrf_token from HTML      │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  STEP 2: ATTEMPT REGISTRATION       │
│  POST /api/v1/web/accounts/...      │
│  Body: {                            │
│    email: "test@gmail.com",         │
│    username: "random123",           │
│    ...                              │
│  }                                  │
│  Headers: {                         │
│    x-csrftoken: <token>             │
│  }                                  │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  STEP 3: ANALYZE RESPONSE           │
│  Response: {                        │
│    "status": "fail",                │
│    "errors": {                      │
│      "email": [{                    │
│        "code": "email_is_taken"     │
│      }]                             │
│    }                                │
│  }                                  │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  STEP 4: DETERMINE RESULT           │
│  "email_is_taken" detected          │
│  → Email EXISTS on Instagram        │
└──────────────┬──────────────────────┘
               │
               ▼
OUTPUT: {
  "name": "instagram",
  "domain": "instagram.com",
  "exists": True,
  "rateLimit": False,
  ...
}
```

---

## 5. Async Execution Model

### Trio Concurrency Architecture:

```
┌──────────────────────────────────────────────────────────────┐
│                   MAIN THREAD (Flask)                        │
│  Handles HTTP requests, serves templates                     │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ threading.Thread(target=run_holehe_async)
                         │
┌────────────────────────▼─────────────────────────────────────┐
│              BACKGROUND THREAD                               │
│  Isolated from Flask to prevent blocking                     │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ trio.run(main)
                         │
┌────────────────────────▼─────────────────────────────────────┐
│                 TRIO ASYNC RUNTIME                           │
│  Manages async tasks, scheduling, and I/O                    │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              trio.open_nursery()                       │ │
│  │  (Parent task that manages all child tasks)            │ │
│  │                                                        │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │ │
│  │  │  Task 1  │ │  Task 2  │ │  Task 3  │ │ Task 120 │ │ │
│  │  │Instagram │ │  Google  │ │ Twitter  │ │ Netflix  │ │ │
│  │  │          │ │          │ │          │ │          │ │ │
│  │  │ await    │ │ await    │ │ await    │ │ await    │ │ │
│  │  │ GET      │ │ POST     │ │ GET      │ │ POST     │ │ │
│  │  │   ▼      │ │   ▼      │ │   ▼      │ │   ▼      │ │ │
│  │  │ await    │ │ await    │ │ await    │ │ await    │ │ │
│  │  │ POST     │ │ Parse    │ │ Parse    │ │ Parse    │ │ │
│  │  │   ▼      │ │   ▼      │ │   ▼      │ │   ▼      │ │ │
│  │  │ append() │ │ append() │ │ append() │ │ append() │ │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ │ │
│  │                                                        │ │
│  │  All tasks execute concurrently, not sequentially!    │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │         httpx.AsyncClient (Shared)                     │ │
│  │  • Connection pooling                                  │ │
│  │  • Automatic retry logic                               │ │
│  │  • Timeout management (30s)                            │ │
│  └────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

### Timing Comparison:

**Sequential (Not Used):**
```
Instagram (0.5s) → Google (0.4s) → Twitter (0.6s) → ... → Netflix (0.3s)
Total: 0.5 + 0.4 + 0.6 + ... + 0.3 = ~60 seconds for 120 sites
```

**Concurrent (Our Implementation):**
```
Instagram (0.5s) ┐
Google (0.4s)    │
Twitter (0.6s)   ├─── All execute simultaneously
...              │
Netflix (0.3s)   ┘
Total: max(0.5, 0.4, 0.6, ..., 0.3) = ~10-30 seconds
```

**Speedup: ~3-6x faster!**

---

## 6. Frontend Polling Mechanism

```
User clicks "Search"
│
▼
┌─────────────────────────────────────┐
│  POST /check_email                  │
│  Response: {"status": "processing"} │
└────────────┬────────────────────────┘
             │
             ▼
┌──────────────────────────────────────┐
│  Start Polling Interval (1 second)   │
│                                      │
│  Timer: 0s  ─┐                       │
│  Timer: 1s   │ GET /get_results     │
│  Timer: 2s   │ GET /get_results     │
│  Timer: 3s   │ GET /get_results     │
│  ...         │ GET /get_results     │
│  Timer: 15s  │ GET /get_results     │
│  Timer: 16s ─┘                       │
│              {"status": "completed"} │
└────────────┬─────────────────────────┘
             │
             ▼
┌──────────────────────────────────────┐
│  Clear Interval, Display Results     │
└──────────────────────────────────────┘

Maximum polling: 120 attempts (2 minutes)
Then timeout with error message
```

---

## 7. Error Handling Flow

```
┌─────────────────────┐
│   HTTP Request      │
└─────────┬───────────┘
          │
          ▼
    ┌─────────┐
    │ Try:    │
    │ await   │
    │ request │
    └────┬────┘
         │
    ┌────▼────────────────────────┐
    │  Possible Outcomes:         │
    ├─────────────────────────────┤
    │ 1. Success (200 OK)         │
    │    → Parse response         │
    │    → Extract data           │
    │    → Return result          │
    ├─────────────────────────────┤
    │ 2. Rate Limit (429)         │
    │    → Mark as rate limited   │
    │    → Set rateLimit: true    │
    ├─────────────────────────────┤
    │ 3. Timeout (30s)            │
    │    → Catch timeout error    │
    │    → Mark as error          │
    ├─────────────────────────────┤
    │ 4. Network Error            │
    │    → Catch exception        │
    │    → Log error              │
    │    → Continue to next       │
    ├─────────────────────────────┤
    │ 5. Invalid Response         │
    │    → Catch parsing error    │
    │    → Mark as error          │
    └─────────────┬───────────────┘
                  │
                  ▼
    ┌──────────────────────────┐
    │  Append result to 'out'  │
    │  (Always returns result) │
    └──────────────────────────┘
```

---

## 8. Cache Management

```
┌─────────────────────────────────────┐
│    RESULTS CACHE (In-Memory Dict)   │
│                                     │
│  {                                  │
│    "test@gmail.com": {              │
│      "status": "completed",         │
│      "timestamp": "2025-11-14...",  │
│      "total_sites": 120,            │
│      "found_accounts": 15,          │
│      "results": [...]               │
│    },                               │
│    "user@yahoo.com": {              │
│      "status": "processing",        │
│      "progress": "45/120",          │
│      "timestamp": "2025-11-14..."   │
│    }                                │
│  }                                  │
└─────────────────────────────────────┘

Lifecycle:
1. New request → Add to cache with "processing"
2. Background processing → Update progress
3. Completion → Update with "completed" + results
4. Frontend polls → Read from cache
5. Manual clear → Remove from cache
6. Server restart → Cache cleared (in-memory)
```

---

## 9. Security Layers

```
┌─────────────────────────────────────────────┐
│           USER INPUT VALIDATION             │
│  • Email format regex                       │
│  • Length limits                            │
│  • Special character sanitization           │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│           NETWORK SECURITY                  │
│  • HTTPS only requests                      │
│  • Timeout protection (30s)                 │
│  • Rate limit detection                     │
│  • Connection pooling limits                │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│          APPLICATION SECURITY               │
│  • No permanent data storage                │
│  • In-memory cache only                     │
│  • Background thread isolation              │
│  • Error message sanitization               │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│           PRIVACY PROTECTION                │
│  • No logging of email addresses            │
│  • Cache cleared on restart                 │
│  • No third-party analytics                 │
└─────────────────────────────────────────────┘
```

---

## Summary

This architecture demonstrates:
- ✅ **Scalable design**: Can handle multiple concurrent requests
- ✅ **Efficient processing**: Async operations reduce total time
- ✅ **User-friendly**: Real-time updates via polling
- ✅ **Maintainable**: Modular structure for easy updates
- ✅ **Robust**: Comprehensive error handling
- ✅ **Secure**: Multiple security layers

---

*This architecture supports 120+ website checks in 10-30 seconds while maintaining responsiveness and user experience.*
